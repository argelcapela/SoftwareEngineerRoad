BY ARGEL CAPELA! AFTER KNOW IT BY BY VSCODE AND SUBLIME TEXT 3 
I LOVE VIIIIMMMMMM!!!!

_____________VIM_______________________________

GOOD THEORY:

// BAD THINGS OF VIM //
1) We can't paste text outside the terminal easily;
2) Just is productive as you master the commands, until it, you will suffer a little bit;



PRACTICE:

// MODES //

ESC   ! normal mode !
i     ! insert mode !


// IN NORMAL MODE //

:q!  				!exit without save!

:w!				!save without exit!

:x!				!??exit and save??!

:<line number>			!go to line tal!

gg				!first line of the document!

GG || $				!last line of the document!

v + <arrow button("setas") 	!select text!

dd  				!delete current line!

yyp				!duplicate current line!

ggVG				!select all text in document!

y				!copy selection!

d				!cut selection!

p				!paste!

u				!undo(desfazer)!

r				!redo(refazer)!

>				!identar(espaço para ficar tudo bonitão)!



// (IN NORMAL MODE) find and replace //

:%s/<find term>/<replace term>/g      !each occurrence in the document!










___________TMUX_______________________________



GOOD THEORY:

// ATTACH/ATTACHED - is like inside the session //

// DETTACH/DETTACHED - you leaf the session, aren't inside it // 



PRACTICE:

// create session !out of tmux or will bug! //
tmux new -s <sessian_name>

// leave tmux
Ctrl + b | d  !! use it to avoid problems saving the sessions!! 

exit


// list sessions !inside any sessian! //
tmux ls

// change session !!   //
tmux a -t <SESSION NAME> !!out of tmux or will bug !!

// kill sessian //
tmux kill-session -t <name> !inside or out whatever!

tmux kill-server

// open new window(pane) //
Ctrl + b | "
Ctrl + b | %

// change current window(pane) //
Ctrl + b | o

// remove current window(pane) //
Ctrl + b | x | type y | enter

// resize window(pane) //

tmux resize-pane  -U <value>   !up!
		  -D <value>   !down!
		  -L <value>   !left!
		  -R <value>   !right!
!!! using for example the -U and value, will be
added <value> i don't know, in direction to -U, and so on, -D the window will go in direction to down, the line dividing will go in direction to down, you understood, now try yourself, test and    !!!

// install tmux //
pacman -S tmux

// new windows(panes) opened in the current directory and others configs //

after install > 

creates the file ~/.tmux.conf and type there >

bind '"' split-window -c "#{pane_current_path}"
bind % split-window -h -c "#{pane_current_path}"
bind c new-window -c "#{pane_current_path}" >

rest the tmux and should work normal

